version: '2'

services:
  web:
    build: .
    command: npm run build && node build/web.js
    volumes:
      - .:/superbowleto
      - /superbowleto/node_modules
    ports:
      - 3000:3000
    environment:
      - API_ENV=test

